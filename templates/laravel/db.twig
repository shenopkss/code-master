# db schema

{% for table in db.tables %}

### {{ table.name }}

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
{% for column in table.columns %}
|{{ column.name }}|{{ column.type}}| 是 | {{column.comment}}|
{% endfor %}
{% endfor %}

