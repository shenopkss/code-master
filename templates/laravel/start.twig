{% for table in db.tables %}
{% set data = {'table': table, 'db' : db} %}
{{ render('laravel/route.twig', data, "#{db.name}/route/#{table.name|camel}.php")}}
{{ render('laravel/controller.twig', data, "#{db.name}/controller/#{table.name|camel}Controller.php")}}
{{ render('laravel/trait.twig', data, "#{db.name}/trait/#{table.name|camel}Restable.php")}}
{{ render('laravel/model.twig', data, "#{db.name}/model/#{table.name|camel}.php")}}
{{ render('laravel/test.twig', data, "#{db.name}/test/#{table.name|camel}Test.php")}}
{{ render('laravel/factory.twig', data, "#{db.name}/factory/#{table.name|camel}Factory.php")}}
<!-- {{ render('laravel/view/create.twig', data, "#{db.name}/view/#{table.name|camel}/create.blade.php")}} -->
<!-- {{ render('laravel/view/edit.twig', data, "#{db.name}/view/#{table.name|camel}/edit.blade.php")}} -->
<!-- {{ render('laravel/view/index.twig', data, "#{db.name}/view/#{table.name|camel}/index.blade.php")}} -->
{{ render('laravel/api/list.twig', data, "#{db.name}/api/#{table.name}/list.md")}}
{{ render('laravel/api/create.twig', data, "#{db.name}/api/#{table.name}/create.md")}}
{{ render('laravel/api/update.twig', data, "#{db.name}/api/#{table.name}/update.md")}}
{{ render('laravel/api/delete.twig', data, "#{db.name}/api/#{table.name}/delete.md")}}
{{ render('laravel/api/find.twig', data, "#{db.name}/api/#{table.name}/find.md")}}
{% endfor %}
{{ render('laravel/db.twig', {'db':db}, "#{db.name}/db.md")}}
